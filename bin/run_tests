#!/bin/bash
set -ex

GOPATH=$(cd "$(dirname "${BASH_SOURCE[0]}")"/../; pwd)
find "$GOPATH/bin" -type f -print0 | xargs -0 shellcheck -s bash -- 
echo "Shellcheck tests passed"

export GOPATH
ginkgo "${GOPATH}/src/kubo-deployment-tests"
